// Simple color mode utility for light/dark theme support
export function useColorModeValue(lightValue: string, _darkValue: string): string {
  // For now, just return light mode values
  // You can extend this later with actual theme context
  return lightValue;
}